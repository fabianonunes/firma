TODO
-- usar pkcs11Module.waitForSlotEvent para detectar a presença de token e atualizar a lista de provider/aliases
-- detectar tamanho do arquivo na hora de assinar: se não for muito grande, não usa arquivo temporário.
-- criar alguma forma de escolher posições absolutas: topleft, topright, center, etc...
-- avisar a quantidade de erros na janela de pin
-- informar se a última tentativa deu senha-inválida na tela de pin
-- avisar em algum lugar pq deu erro naquele arquivo (?)

-- esc key
-- i18n nome das colunas
-- botão para mesclar assinaturas existentes (free-them)
-- não permitir arquivos duplicados
-- criar seletor de última/primeira página
-- sumir com a drop-area

EXCEÇÕES
-- certificados sem chave privada
-- fechar o aplicativo após remover o token
-- cancelar a janela do pin
-- fechar aplicativo no meio de uma Task de assinatura